# LIFEFlow Architecture & Technical Design

## 1. Architectural Philosophy
LIFEFlow is engineered as a monolithic, modular Django application prioritizing data integrity, strict auditability, deterministic state machines, and concurrency safety.

Microservices, Kafka, and separate frontend frameworks (e.g. React/Node) were explicitly avoided to eliminate network partitioning hazards, eventual consistency gaps, and unnecessary operational complexity in mission-critical blood transfusion workflows.

## 2. Component Hierarchy
`
??????????????????????????????????????????????????????????
?                   Presentation Layer                   ?
?        Django Templates + Bootstrap 5 + Vanilla JS     ?
??????????????????????????????????????????????????????????
                            ?
??????????????????????????????????????????????????????????
?                    Application Layer                   ?
?       Role Decorators, Forms, Services, Exporters      ?
??????????????????????????????????????????????????????????
                            ?
??????????????????????????????????????????????????????????
?                      Domain Layer                      ?
?        15 Modular Django Apps (State Machine ORM)       ?
??????????????????????????????????????????????????????????
                            ?
??????????????????????????????????????????????????????????
?                   Data & Audit Layer                   ?
?    PostgreSQL (Production) / SQLite (Dev) + AuditLog   ?
??????????????????????????????????????????????????????????
`

## 3. Separation of Responsibilities (15 Apps)
1. **core**: Organization identity, regulatory parameter settings, error handling.
2. **udit**: Global thread-local audit middleware, immutable event log for every state transition.
3. **ccounts**: Role-based access control (RBAC), multi-role profile mappings, dashboard dispatcher.
4. **hospitals**: Affiliated institutions, hospital contacts, verification lifecycle.
5. **patients**: Recipient identity, medical records, hospital MRN tracking.
6. **donors**: Voluntary donor registry, demographic records, deferral statuses.
7. **ppointments**: Scheduling, calendar slot validation, check-in automation.
8. **camps**: Community blood drives, venue management, mobile phlebotomy logistics.
9. **donations**: Phlebotomy collection events, volume accounting, adverse reaction tracking.
10. **laboratory**: Serological test entry, TTI screening gates, medical officer verification.
11. **lood_components**: Fractionation and separation into PRBC, FFP, Platelets, Cryo.
12. **inventory**: Unit-level tracking, storage hierarchy (Area -> Device -> Position), cold-chain logging.
13. **
equests_app**: Clinical requisitions, atomic reservations (select_for_update), issue and return triage.
14. **
otifications**: Asynchronous/in-app alert queue for cold-chain alarms, expiration, STAT requisitions.
15. **
eports**: Reporting and regulatory audit export suite (CSV & ReportLab PDF).

## 4. Concurrency & Integrity Model
- **Atomic Reservation Workflow:** When reserving a unit, LIFEFlow wraps the operation inside 	ransaction.atomic() and locks the candidate row using select_for_update(). If two users request the same unit at the same millisecond, the database serializes access, preventing double allocations.
- **Quarantine Safety Gate:** Quarantined or expired units are strictly filtered out of the issuable pool at the ORM query level and validated on the model layer (is_issuable).
